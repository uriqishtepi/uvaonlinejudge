task=${1}
make ${task}
if [ $? -eq 0 ] ; then 
   echo "running $1 < $1.in"
   ./${task} < ${task}.in | tee /tmp/myout.txt && diff -q ${task}.out /tmp/myout.txt || echo run " vimdiff /tmp/myout.txt ${task}.out" to see diff
fi

